### 一些约定

#### c-Majorana

$$
\begin{equation}
\bm{\psi}_c
=\begin{pmatrix}
\bm{c}_A \\
\bm{c}_B
\end{pmatrix}
=\begin{pmatrix}
c_{1,A} \\
\vdots \\
c_{N,A} \\
c_{1,B} \\
\vdots \\
c_{N,B}
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_c^\dag
=\begin{pmatrix}
\bm{c}_A^\dag &\bm{c}_B^\dag
\end{pmatrix}
=\begin{pmatrix}
c_{1,A}^\dag &\cdots &c_{N,A}^\dag &c_{1,B}^\dag &\cdots &c_{N,B}^\dag
\end{pmatrix}
=\begin{pmatrix}
c_{1,A} &\cdots &c_{N,A} &c_{1,B} &\cdots &c_{N,B}
\end{pmatrix}
\end{equation}
$$

其中 $c_{i,A}^\dag=c_{i,A},c_{i,B}^\dag=c_{i,B} .$

对于 $c_{i,A} $，$i $ 是 unit cell 指标，$A $ 是 unit cell 内 A 类格点。

#### a 费米子

每个 unit cell 可定义一种复费米子模式

$$
\begin{equation}
a_i = \frac{1 }{2 } \left(c_{i,A} + \mathrm{i}c_{i,B} \right),\quad
a_i^\dag = \frac{1 }{2 } \left(c_{i,A} - \mathrm{i}c_{i,B} \right)
\end{equation}
$$

$a $ 费米子的模式数等于元胞数 $N $。

$$
\begin{equation}
\bm{a}
=\begin{pmatrix}
a_1 \\
\vdots \\
a_N
\end{pmatrix},\quad
\bm{a}^\dag
=\begin{pmatrix}
a_1^\dag &\cdots &a_N^\dag
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a
=\begin{pmatrix}
\bm{a} \\
\left(\bm{a}^\dag \right)^\top
\end{pmatrix}
=\begin{pmatrix}
a_1 \\
\vdots \\
a_N \\
a_1^\dag \\
\vdots \\
a_N^\dag
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a^\dag
=\begin{pmatrix}
a_1^\dag &\cdots &a_N^\dag
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a^\dag
=\begin{pmatrix}
\bm{a}^\dag &\bm{a}^\top
\end{pmatrix}
=\begin{pmatrix}
a_1^\dag &\cdots &a_N^\dag &a_1 &\cdots &a_N
\end{pmatrix}
\end{equation}
$$

### c-Majorana 与 a 费米子

#### $\bm{\psi}_c $ 与 $\bm{\psi}_a $ 的关系

$$
\begin{equation}
a_i = \frac{1 }{2 } \left(c_{i,A} + \mathrm{i}c_{i,B} \right),\quad
a_i^\dag = \frac{1 }{2 } \left(c_{i,A} - \mathrm{i}c_{i,B} \right)
\end{equation}
$$

$$
\begin{equation}
c_{i,A}
=a_{i} + a^\dag_{i},\quad
c_{i,B}
=\frac{1 }{\mathrm{i} } \left(a_{i} - a^\dag_{i} \right),
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_c
=\begin{pmatrix}
\bm{c}_A \\
\bm{c}_B
\end{pmatrix}
=\begin{pmatrix}
c_{1,A} \\
\vdots \\
c_{N,A} \\
c_{1,B} \\
\vdots \\
c_{N,B}
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a
=\begin{pmatrix}
\bm{a} \\
\left(\bm{a}^\dag \right)^\top
\end{pmatrix}
=\begin{pmatrix}
a_1 \\
\vdots \\
a_N \\
a_1^\dag \\
\vdots \\
a_N^\dag
\end{pmatrix}
\end{equation}
$$

下面开算。

$$
\begin{equation}
\begin{aligned}
\bm{a}
\equiv \begin{pmatrix}
a_1 \\
\vdots \\
a_N
\end{pmatrix}
=\begin{pmatrix}
\frac{1 }{2 } c_{1,A} + \frac{\mathrm{i} }{2 } c_{1,B}  \\
\vdots \\
\frac{1 }{2 } c_{N,A} + \frac{\mathrm{i} }{2 } c_{N,B}
\end{pmatrix}
=\frac{1 }{2 }
\begin{pmatrix}
c_{1,A} \\
\vdots \\
c_{N,A}
\end{pmatrix} + \frac{\mathrm{i} }{2 } 
\begin{pmatrix}
c_{1,B} \\
\vdots \\
c_{N,B}
\end{pmatrix}
=\frac{1 }{2 } \bm{c}_A + \frac{\mathrm{i} }{2 } \bm{c}_B
\end{aligned}
\end{equation}
$$

$$
\begin{equation}
\bm{a}^\dag
=\frac{1 }{2 } \bm{c}_A^\dag - \frac{\mathrm{i} }{2 } \bm{c}_B^\dag
\end{equation}
$$

$$
\begin{equation}
\left(\bm{a}^\dag \right)^\top
=\frac{1 }{2 } \bm{c}_A - \frac{\mathrm{i} }{2 } \bm{c}_B
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a
=\begin{pmatrix}
\bm{a} \\
\left(\bm{a}^\dag \right)^\top
\end{pmatrix}
=\begin{pmatrix}
\frac{1 }{2 } \bm{c}_A + \frac{\mathrm{i} }{2 } \bm{c}_B \\[1mm]
\frac{1 }{2 } \bm{c}_A - \frac{\mathrm{i} }{2 } \bm{c}_B
\end{pmatrix}
=\begin{pmatrix}
\frac{1 }{2 } \bm{I} &\frac{\mathrm{i} }{2 } \bm{I} \\[1mm]
\frac{1 }{2 } \bm{I} &-\frac{\mathrm{i} }{2 } \bm{I}
\end{pmatrix}
\begin{pmatrix}
\bm{c}_A \\[1mm]
\bm{c}_B
\end{pmatrix}
\equiv \bm{T}_{ac} \bm{\psi}_c
\end{equation}
$$

$$
\begin{equation}
\bm{T}_{ac}^{-1}
=\begin{pmatrix}
\bm{I} &\bm{I} \\
-\mathrm{i}\bm{I} &\mathrm{i}\bm{I}
\end{pmatrix}
\equiv \bm{T}_{ca}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_c 
=\bm{T}_{ac}^{-1} \bm{\psi}_a
=\bm{T}_{ca} \bm{\psi}_a,\quad
\bm{T}_{ca}
=\begin{pmatrix}
\bm{I} &\bm{I} \\
-\mathrm{i}\bm{I} &\mathrm{i}\bm{I}
\end{pmatrix}
\end{equation}
$$

#### 由 c 和 a 表达的 BdG 哈密顿量之间的转换

用 Nambu 旋量表达哈密顿量时，中间的 kernel 矩阵不唯一。

特别地，若存在矩阵 $\bm{H}_{\mathrm{test}} $ 使得

$$
\begin{equation}
H_u = \frac{\mathrm{i} }{2 }  \bm{\psi}_c^\dag \bm{H}_{\mathrm{test}} \bm{\psi}_c,
\end{equation}
$$

则由 $c_i c_j = -c_j c_i,i\ne j $ 可得

$$
\begin{equation}
H_u
=\frac{\mathrm{i} }{2 }  \bm{\psi}_c^\dag \left(-\bm{H}_{\mathrm{test}}^\top \right) \bm{\psi}_c
\end{equation}
$$

因此有

$$
\begin{equation}
H_u
=\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \left(\frac{\bm{H}_{\mathrm{test}} - \bm{H}_{\mathrm{test}}^\top }{2 }  \right) \bm{\psi}_c
\end{equation}
$$

此时中间的 kernel 矩阵是反对称的。

所以中间 kernel 矩阵的构造是有讲究的，我们需要它是反对称。如果写出一个用 c 的 Nambu 旋量表达的哈密顿量，但中间的 kernel 矩阵不是反对称的，则需要反对称化一下。至于反对称的 kernel 矩阵有什么优势？其优势在把 Majorana 重新组合成费米子时才能看出来。

假设我们已经找到了反对称的 kernel 矩阵，并且这个矩阵是实的：

$$
\begin{equation}
H_u
=\frac{\mathrm{i} }{2 }  \bm{\psi}_c^\dag \bm{H}_c \bm{\psi}_c,\quad \bm{H}_c^\top
=-\bm{H}_c,\quad\bm{H}_c\in \mathbb{R}_{2N\times 2N}
\end{equation}
$$

可以验证哈密顿量的厄米性：

$$
\begin{equation}
H_u^\dag
=-\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \left(\bm{H}_c \right)^\dag \bm{\psi}_c 
=\frac{\mathrm{i} }{2 }  \bm{\psi}_c^\dag \bm{H}_c \bm{\psi}_c
=H_u
\end{equation}
$$

接下来看看怎么用 a 的 Nambu 旋量表达这个哈密顿量。

$$
\begin{equation}
\bm{\psi}_c 
=\bm{T}_{ca} \bm{\psi}_a,\quad
\bm{T}_{ca}
=\begin{pmatrix}
\bm{I} &\bm{I} \\
-\mathrm{i}\bm{I} &\mathrm{i}\bm{I}
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_c^\dag
=\bm{\psi}_a^\dag \bm{T}_{ca}^\dag
\end{equation}
$$

于是

$$
\begin{equation}
H_u
=\frac{\mathrm{i} }{2 }  \bm{\psi}_c^\dag \bm{H}_c \bm{\psi}_c
=\frac{\mathrm{i} }{2 }  \bm{\psi}_a^\dag \bm{T}_{ca}^\dag \bm{H}_c \bm{T}_{ca} \bm{\psi}_a
=\frac{1 }{2 }  \bm{\psi}_a^\dag \left(\mathrm{i} \bm{T}_{ca}^\dag \bm{H}_c \bm{T}_{ca} \right) \bm{\psi}_a
\equiv \frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a \bm{\psi}_a,\quad
\bm{H}_a
=\mathrm{i} \bm{T}_{ca}^\dag \bm{H}_c \bm{T}_{ca}
\end{equation}
$$

利用 $\bm{H}_c $ 的实反对称性，可以验证 $\bm{H}_a $ 的厄米性：

$$
\begin{equation}
\bm{H}_a^\dag
=-\mathrm{i} \bm{T}_{ca}^\dag \left(\bm{H}_c \right)^\dag \bm{T}_{ca}
=-\mathrm{i} \bm{T}_{ca}^\dag \left(\bm{H}_c \right)^\top \bm{T}_{ca}
=\mathrm{i} \bm{T}_{ca}^\dag \bm{H}_c  \bm{T}_{ca}
=\bm{H}_a
\end{equation}
$$

#### 费米子 BdG 哈密顿量粒子-空穴对称性

可以证明，由 Majorana BdG 哈密顿量

$$
\begin{equation}
H_u
=\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \bm{H}_c \bm{\psi}_c,\quad
\bm{H}_c^* = \bm{H}_c,\quad
\bm{H}_c^\top = -\bm{H}_c
\end{equation}
$$

转换到费米子 $a $ BdG 哈密顿量

$$
\begin{equation}
H_u
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a \bm{\psi}_a
\end{equation}
$$

此时 $\bm{H}_a $ 不仅是厄米的，而且有如下的分块性质：

$$
\begin{equation}
\bm{H}_a
=\begin{pmatrix}
\Xi &\Delta \\
\Delta^\dag &-\Xi^\top
\end{pmatrix},\quad
\Xi^\dag = \Xi,\quad
\Delta^\top = -\Delta
\end{equation}
$$

证明：



### a 费米子与 Bogoliubov 准粒子 $\alpha $

考虑费米子 BdG 哈密顿量

$$
\begin{equation}
H_u
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a \bm{\psi}_a,\quad
\bm{H}_a^\dag = \bm{H}_a
\end{equation}
$$

由费米子 BdG 粒子-空穴对称性，可以把厄米矩阵 $\bm{H}_a $ 对角化成如下形式：

$$
\begin{equation}
\bm{H}_a
=\bm{U}_a \bm{D}_a \bm{U}_a^\dag
\end{equation}
$$

$$
\begin{equation}
\bm{D}_a
=\mathrm{diag}\left(\bm{E}_a^+ , \bm{E}_a^- \right)
=\mathrm{diag}\left(E_1,\cdots,E_N,-E_1,\cdots,-E_N \right),\quad
0<E_1<\cdots<E_N
\end{equation}
$$

$$
\begin{equation}
\bm{U}_a
=\begin{pmatrix}
\bm{W}_a &\bm{V}_a^* \\
\bm{V}_a &\bm{W}_a^*
\end{pmatrix},\quad
\bm{U}^\dag
=\begin{pmatrix}
\bm{W}_a^\dag &\bm{V}_a^\dag \\
\bm{V}_a^\top &\bm{W}_a^\top
\end{pmatrix}
\end{equation}
$$

定义 $\alpha $ 准粒子 Nambu 旋量：

$$
\begin{equation}
\bm{\psi}_\alpha
=\bm{U}_a^\dag \bm{\psi}_a,\quad
\bm{\psi}_\alpha^\dag
=\bm{\psi}_a^\dag \bm{U}_a
\end{equation}
$$

$$
\begin{equation}
\begin{pmatrix}
\bm{\alpha} \\
\left(\bm{\alpha}^\dag \right)^\top
\end{pmatrix}
=\begin{pmatrix}
\bm{W}_a^\dag &\bm{V}_a^\dag \\
\bm{V}_a^\top &\bm{W}_a^\top
\end{pmatrix}
\begin{pmatrix}
\bm{a} \\
\left(\bm{a}^\dag \right)^\top
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\begin{pmatrix}
\bm{\alpha}^\dag &\bm{\alpha}^\top
\end{pmatrix}
=\begin{pmatrix}
\bm{a}^\dag &\bm{a}^\top
\end{pmatrix}
\begin{pmatrix}
\bm{W}_a &\bm{V}_a^* \\
\bm{V}_a &\bm{W}_a^*
\end{pmatrix}
\end{equation}
$$

则 a 费米子 BdG 哈密顿量就可写成 $\alpha $ 费米子自由费米子哈密顿量：

$$
\begin{equation}
\begin{aligned}
H_u
&=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a \bm{\psi}_a
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{U}_a \bm{D}_a \bm{U}_a^\dag \bm{\psi}_a
=\frac{1 }{2 } \bm{\psi}_\alpha^\dag \bm{D}_a \bm{\psi}_\alpha \\
&=\frac{1 }{2 } 
\begin{pmatrix}
\bm{\alpha}^\dag &\bm{\alpha}^\top
\end{pmatrix}
\begin{pmatrix}
\bm{E}_a^+ &0 \\
0 &\bm{E}_a^-
\end{pmatrix}
\begin{pmatrix}
\bm{\alpha} \\
\left(\bm{\alpha}^\dag \right)^\top
\end{pmatrix} \\
&=\frac{1 }{2 } \left[\bm{\alpha}^\dag \bm{E}_a^+ \bm{\alpha} + \bm{\alpha}^\top \bm{E}_a^- \left(\bm{\alpha}^\dag \right)^\top \right] \\
&=\frac{1 }{2 } \left[\sum_{i=1}^{N} E_i \alpha_i^\dag \alpha_i + \sum_{i=1}^{N} \left(-E_i \right) \alpha_i \alpha_i^\dag \right] \\
&=\frac{1 }{2 } \left[\sum_{i=1}^{N} E_i \alpha_i^\dag \alpha_i + \sum_{i=1}^{N} \left(-E_i \right) \left(1 - \alpha_i^\dag \alpha_i \right) \right] \\
&=\sum_{i=1}^{N} E_i \alpha_i^\dag \alpha_i - \frac{1 }{2 } \sum_{i=1}^{N} E_i
\end{aligned}
\end{equation}
$$

则基态是 $\alpha $ 费米子占据数都为 $0 $ 的态，基态能量为

$$
\begin{equation}
E_{\mathrm{GS}}
=-\frac{1 }{2 } \sum_{i=1}^{N} E_i
\end{equation}
$$

### 标准规范构型下的 Bogoliubov 准粒子 $\alpha[u_0] $ 以及 c 与 $\alpha[u_0] $ 的关系

#### $\alpha[u_0] $ 与 $a $ 的关系

考虑标准规范构型 $u_{0} $ 下的由 c 表达的哈密顿量

$$
\begin{equation}
H\left[u_{0} \right]
=\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \bm{H}_c\left[u_{0} \right] \bm{\psi}_c
\end{equation}
$$

可以写为 $a $ 费米子的 BdG 哈密顿量：

$$
\begin{equation}
H\left[u_{0} \right]
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a\left[u_{0} \right] \bm{\psi}_a
\end{equation}
$$

$$
\begin{equation}
\bm{H}_a\left[u_{0} \right]
=\mathrm{i} \bm{T}_{ca}^\dag \bm{H}_c\left[u_{0} \right] \bm{T}_{ca},\quad
\bm{T}_{ca}
=\begin{pmatrix}
\bm{I} &\bm{I} \\
-\mathrm{i}\bm{I} &\mathrm{i}\bm{I}
\end{pmatrix}
\end{equation}
$$

$u_{0} $ 构型下对 $a $ 费米子 BdG 哈密顿量的 kernel 矩阵 $\bm{H}_a\left[u_{0} \right] $ 对角化：

$$
\begin{equation}
\bm{H}_a\left[u_{0} \right]
=\bm{U}_a\left[u_{0} \right] \bm{D}\left[u_{0} \right] \bm{U}_a^\dag\left[u_{0} \right]
\end{equation}
$$

$$
\begin{equation}
H\left[u_{0} \right]
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{H}_a\left[u_{0} \right] \bm{\psi}_a
=\frac{1 }{2 } \bm{\psi}_a^\dag \bm{U}_a\left[u_{0} \right] \bm{D}\left[u_{0} \right] \bm{U}_a^\dag\left[u_{0} \right] \bm{\psi}_a
=\frac{1 }{2 } \bm{\psi}_{\alpha(0)}^\dag \bm{D}\left[u_0 \right] \bm{\psi}_{\alpha(0)}
\end{equation}
$$

构造出的准粒子记为 $\alpha[u_0] $：

$$
\begin{equation}
\bm{\psi}_{\alpha[u_0]}
=\bm{U}_a^\dag\left[u_0 \right] \bm{\psi}_a,\quad
\bm{\psi}_{\alpha[u_0]}^\dag
=\bm{\psi}_a^\dag \bm{U}_a\left[u_0 \right]
\end{equation}
$$

$$
\begin{equation}
\begin{pmatrix}
\bm{\alpha}[u_0] \\
\left(\bm{\alpha}^\dag[u_0] \right)^\top
\end{pmatrix}
=\begin{pmatrix}
\bm{W}_a^\dag\left[u_0 \right] &\bm{V}_a^\dag\left[u_0 \right] \\
\bm{V}_a^\top\left[u_0 \right] &\bm{W}_a^\top\left[u_0 \right]
\end{pmatrix}
\begin{pmatrix}
\bm{a} \\
\left(\bm{a}^\dag \right)^\top
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\begin{pmatrix}
\bm{\alpha}^\dag[u_0] &\bm{\alpha}^\top[u_0]
\end{pmatrix}
=\begin{pmatrix}
\bm{a}^\dag &\bm{a}^\top
\end{pmatrix}
\begin{pmatrix}
\bm{W}_a\left[u_0 \right] &\bm{V}_a^*\left[u_0 \right] \\
\bm{V}_a\left[u_0 \right] &\bm{W}_a^*\left[u_0 \right]
\end{pmatrix}
\end{equation}
$$

$\bm{\psi}_a $ 可由 $\bm{\psi}_{\alpha[u_0]} $ 表达：

$$
\begin{equation}
\bm{\psi}_{\alpha[u_0]}
=\bm{U}_a^\dag\left[u_0 \right] \bm{\psi}_a,\quad
\bm{\psi}_{\alpha[u_0]}^\dag
=\bm{\psi}_a^\dag \bm{U}_a\left[u_0 \right]
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a
=\bm{U}_a\left[u_0 \right] \bm{\psi}_{\alpha[u_0]},\quad
\bm{\psi}_a^\dag
=\bm{\psi}_{\alpha[u_0]}^\dag U_a^\dag[u_0]
\end{equation}
$$

#### $\alpha[u_0] $ 与 c 的关系

$$
\begin{equation}
\bm{\psi}_c 
=\bm{T}_{ca} \bm{\psi}_a,\quad
\bm{\psi}_c^\dag
=\bm{\psi}_a^\dag \bm{T}_{ca}^\dag,\quad
\bm{T}_{ca}
=\begin{pmatrix}
\bm{I} &\bm{I} \\
-\mathrm{i}\bm{I} &\mathrm{i}\bm{I}
\end{pmatrix}
\end{equation}
$$

$$
\begin{equation}
\bm{\psi}_a
=\bm{U}_a\left[u_0 \right] \bm{\psi}_{\alpha[u_0]},\quad
\bm{\psi}_a^\dag
=\bm{\psi}_{\alpha[u_0]}^\dag \bm{U}_a^\dag[u_0]
\end{equation}
$$

于是

$$
\begin{equation}
\boxed{
\bm{\psi}_c 
=\bm{T}_{ca} \bm{\psi}_a
=\bm{T}_{ca} \bm{U}_a\left[u_0 \right] \bm{\psi}_{\alpha[u_0]}
,\quad
\bm{\psi}_c^\dag
=\bm{\psi}_a^\dag \bm{T}_{ca}^\dag
=\bm{\psi}_{\alpha[u_0]}^\dag \bm{U}_a^\dag[u_0] \bm{T}_{ca}^\dag
}
\end{equation}
$$

#### 在 $u $ 构型下用 $\alpha[u_0] $ 费米子表达哈密顿量

原来有一个用 c-Majorana 表达的哈密顿量，然后把这个哈密顿量写成 $a $ 费米子的 BdG 哈密顿量，然后对角化得到能谱。现在想把这个哈密顿量写成 $\alpha[u_0] $ 费米子的 BdG 哈密顿量，再对角化得到能谱。

设 $u $ 构型下哈密顿量的 c 表示为

$$
\begin{equation}
H[u]
=\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \bm{H}_c[u] \bm{\psi}_c
\end{equation}
$$

利用 c 与 $\alpha[u_0] $ 费米子的关系

$$
\begin{equation}
\bm{\psi}_c 
=\bm{T}_{ca} \bm{\psi}_a
=\bm{T}_{ca} \bm{U}_a\left[u_0 \right] \bm{\psi}_{\alpha[u_0]}
,\quad
\bm{\psi}_c^\dag
=\bm{\psi}_a^\dag \bm{T}_{ca}^\dag
=\bm{\psi}_{\alpha[u_0]}^\dag \bm{U}_a^\dag[u_0] \bm{T}_{ca}^\dag
\end{equation}
$$

则哈密顿量可表达成 $\alpha[u_0] $ 费米子的 BdG 形式

$$
\begin{equation}
\begin{aligned}
H[u]
&=\frac{\mathrm{i} }{2 } \bm{\psi}_c^\dag \bm{H}_c[u] \bm{\psi}_c \\
&=\frac{\mathrm{i} }{2 } \bm{\psi}_{\alpha[u_0]}^\dag \bm{U}_a^\dag[u_0] \bm{T}_{ca}^\dag \bm{H}_c[u] \bm{T}_{ca} \bm{U}_a\left[u_0 \right] \bm{\psi}_{\alpha[u_0]} \\
&=\frac{1 }{2 } \bm{\psi}_{\alpha[u_0]}^\dag \left(\mathrm{i} \bm{U}_a^\dag[u_0] \bm{T}_{ca}^\dag \bm{H}_c[u] \bm{T}_{ca} \bm{U}_a\left[u_0 \right] \right) \bm{\psi}_{\alpha[u_0]} \\
&\equiv \frac{1 }{2 } \bm{\psi}_{\alpha[u_0]}^\dag \bm{H}_{\alpha[u_0]}[u] \bm{\psi}_{\alpha[u_0]} \\
\end{aligned}
\end{equation}
$$

$$
\begin{equation}
\bm{H}_{\alpha[u_0]}[u]
=\mathrm{i} \bm{U}_a^\dag[u_0] \bm{T}_{ca}^\dag \bm{H}_c[u] \bm{T}_{ca} \bm{U}_a\left[u_0 \right]
\end{equation}
$$

对角化 $\bm{H}_{\alpha[u_0]}[u] $ 得到本征值，然后就能得到基态能量。




