# 原子物理中的量纲结构和数量级

## 量纲和单位分析

长度 $L$，米，$\mathrm{m}$

质量 $M$，千克，$\mathrm{kg}$

时间 $t$，秒，$\mathrm{s}$

电流 $I$，安（培），$\mathrm{A}$

热力学温度 $T$，开（尔文），$\mathrm{K}$

发光强度，坎（德拉），$\mathrm{cd}$

物质的量，摩（尔），$\mathrm{mol}$

量子物理学中的基本量纲：$[M],[L],[t],([k_B]) $

其他物理量 $A $ 的物理量的量纲和位数表示成它们不同指数形式的乘积：

$$
[A]
=[M]^\alpha[L]^\beta[t]^\gamma
$$

单摆：已知重力加速度 $g$，摆长 $l$，小球质量 $m$，求单摆周期 $T$ 的形式：

$$
[g]
=[L][t]^{-2},
[l]
=[L],~~
[m]
=[M]
$$

设 $[T]=C[g]^\alpha[l]^\beta[m]^\gamma $，其中 $C$ 是系数

则：

$$
[T]
=C[L]^{\alpha+\beta}[M]^{\gamma}[t]^{-2\alpha}
$$

另一方面，$[T]=[t]$，于是：

$$
\left\{
\begin{aligned}
&\alpha+\beta=0 \\
&\gamma=0 \\
&-2\alpha=1
\end{aligned}
\right.
$$

解得：

$$
\left\{
\begin{aligned}
&\alpha
=-\frac{1}{2} \\
&\beta
=\frac{1}{2} \\
&\gamma
=0
\end{aligned}
\right.
$$

于是：

### 自然量纲

$\hbar$ 标志微观量子特征

$$
[\hbar]
=\frac{[M][L]^2}{[t]}
$$

$c$ 标志狭义相对论特征

$$
[c]
=\frac{[L]}{[t]}
$$

玻尔兹曼常数 $k_B$：

$$
[k_B]
=\frac{[E]}{[T]}
$$

在万有引力存在的弯曲时空中：

$$
[G]
=\frac{[L]^3}{[M][t]^2}
$$



$$
[A]
=C[\hbar]^\alpha[c]^\beta[G]^\gamma[k_B]^\delta
$$

凑出普朗克长度


## 原子物理中的特征量

## 黑体辐射









# 基本概念

## 斯特恩-盖拉赫实验

## 右矢、左矢和算符

一个物理态用一个复矢量空间的态矢量表示，称这样的一个矢量为一个右矢，用 $\ket{\alpha}$ 表示

$c\ket{\alpha}$ 和 $\ket{\alpha}$ 在 $c\ne$ 时表示同一个物理态

一个可观测量可用所涉及矢量空间中的算符来表示。算符从左边作用于一个右矢，结果得到一个右矢

算符 $A$ 的本征右矢，记为 $\ket{\alpha'},\ket{\alpha''},\ket{\alpha'''},\cdots $，是具有如下性质的右矢：

$$
A\ket{\alpha'}
=\alpha'\ket{\alpha'},~~
A\ket{\alpha''}
=\alpha''\ket{\alpha''},~~
A\ket{\alpha'''}
=\alpha'''\ket{\alpha'''},~~
\cdots
$$

由本征右矢表示的物理态称为本征态

考虑一个由可观测量 $A$ 的 $N$ 个本征右矢所张成的 $N$ 维矢量空间，则任意一个右矢 $\ket{\alpha}$ 都可以用 $\alpha',\alpha'',\cdots$ 写成：

$$
\ket{\alpha}
=\sum_{\alpha'} c_{\alpha'}\ket{\alpha'}
$$

对于每个右矢空间中的矢量 $\ket{\alpha}$，都能在这个右矢空间的对偶空间（称为左矢空间）中找到一个用 $\bra{\alpha}$ 的左矢。

一个左矢 $\bra{\beta}$ 和一个右矢 $\ket{\alpha} $ 的内积，记为 $\braket{\beta|\alpha}$，是一个复数，且具有如下性质：

$$
\braket{\beta|\alpha}
=\braket{\alpha|\beta}^*
$$

$$
\braket{\alpha|\alpha}
\geqslant 0
$$

称两个右矢 $\ket{\alpha},\ket{\beta} $ 是正交的，若 $\braket{\alpha|\beta}=0$

给定一个非零的右矢 $\ket{\alpha}$，可以构造出一个归一的右矢：

$$
\ket{\tilde{\alpha}}
\equiv \frac{1}{\sqrt{\braket{\alpha|\alpha}}}\ket{\alpha}
$$

它具有如下性质：

$$
\braket{\tilde{\alpha}|\tilde{\alpha}}
=1
$$

算符从左边作用于一个右矢，结果得到一个右矢

算符从右边作用于一个左矢，结果得到一个左矢

算符 $X$ 的厄米共轭，记为 $X^\dag$，定义为：

$$
X\ket{\alpha}
\mathop{\leftrightarrow}\limits^{\mathrm{DC}}
\bra{\alpha}X^\dag
$$

称一个算符 $X$ 为厄米算符，若 $X=X^\dag$



# 量子动力学

# 角动量理论

# 量子力学中的对称性

# 近似方法

# 散射理论

# 全同粒子

